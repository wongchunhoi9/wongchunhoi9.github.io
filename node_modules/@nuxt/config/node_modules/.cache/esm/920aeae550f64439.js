let path,FMMode;_50f‍.x([["default",()=>_50f‍.o]]);_50f‍.w("path",[["default",["path"],function(v){path=v}]]);_50f‍.w("frontmatter-markdown-loader/mode",[["default",["FMMode"],function(v){FMMode=v}]]);

 
// const baseUrl = 'https://wongchunhoi9.github.io';

_50f‍.d({
  mode: 'universal',
  /*
  ** Headers of the page
  */
  head: {
    title:'wongchunhoi9.com',
    meta: [
      { charset: 'utf-8' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1' },
      { hid: 'description', name: 'description', content: process.env.npm_package_description || '' }
    ],
    link: [
      { rel: 'icon', type: 'image/x-icon', href: '/favicon.ico' },
      { rel: 'stylesheet' , href: "https://fonts.googleapis.com/css?family=Noto+Sans+TC&display=swap"},
      { rel: 'stylesheet' , href: "https://fonts.googleapis.com/css?family=Cutive+Mono|Rajdhani:500|Raleway:400&display=swap" }
    ]
  },
  /*
  ** Customize the progress-bar color
  */
  loading: { color: '#fff' },
  // pageTransition: {
  //   name: 'page',
  //   mode: 'out-in',
  //   type:"animation",
  //   beforeEnter (el) {
  //     console.log('Before enter...');
  //   }
  // },
  /*
  ** Global CSS
  */
  css: ['github-markdown-css' ,
        '~/assets/styles/main.css' ],

        
  /*
  ** Plugins to load before mounting the App
  */
  plugins: [
    '~plugins/date-filter.js',
    '~plugins/core-component.js'
  ],
  /*
  ** Nuxt.js dev-modules
  */
  buildModules: [
  ],
  /*
  ** Nuxt.js modules
  */
  modules: [
    // Doc: https://bootstrap-vue.js.org
    'bootstrap-vue/nuxt',
    // Doc: https://axiosjs.org/usage
    '@nuxtjs/axios'
  ],
  /*
  ** Axios module configuration
  ** See https://axios.nuxtjs.org/options
  */
  axios: {
  },
  /*
  ** Build configuration
  */
  build: {
    /*
    ** You can extend webpack config here
    */
    extend (config, ctx) {
      const vueLoader = config.module.rules.find(rule => rule.loader === 'vue-loader')
      vueLoader.options.transformAssetUrls = {
        video: ['src', 'poster'],
        source: 'src',
        img: 'src',
        image: 'xlink:href',
        'b-img': 'src',
        'b-img-lazy': ['src', 'blank-src'],
        'b-card': 'img-src',
        'b-card-img': 'src',
        'b-card-img-lazy': ['src', 'blank-src'],
        'b-carousel-slide': 'img-src',
        'b-embed': 'src'
      };
    
      config.module.rules.push(
        {
          test: /\.md$/,
          loader: "frontmatter-markdown-loader",
           include: path.resolve(__dirname, 'contents'),
          options: {
            // mode: [FMMode.VUE_COMPONENT],
            mode: [FMMode.VUE_COMPONENT, FMMode.META, FMMode.HTML],
            vue: {
              root: 'markdown-body'
            }
          }
        }
      );
      
    },

  

  }, 

  generate: {
    fallback:true
  },

  server: { 
    port: 3000, // default: 3000
    host: '0.0.0.0' // default: localhost
  }
});
